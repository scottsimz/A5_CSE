#include <stdio.h>
#include <stdlib.h>

int main(){

	//This program will take as input a grid and the current timestamp
	//It will then scan the grid for active elements
	//record the current state of the grid
	//save it as a timestamped csv file
	//update the traffic lights
	//create an array of car elements that car_calculations will call
	//

}

void scan_grid(char** grid, double timestamp){
	//Scan the grid and return an array consisting of all active cars
	//each car struct will have all the information that is needed
	//by car_calculations() to update the kinematics



}